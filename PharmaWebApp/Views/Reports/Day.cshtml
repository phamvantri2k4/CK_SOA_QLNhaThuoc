@model PharmaWebApp.Models.SalesReportViewModel
@{
    ViewData["Title"] = "Báo cáo ngày";
}

<div class="py-2">
    <div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-3">
        <div>
            <div class="app-page-title"><i class="bi bi-graph-up"></i> Báo cáo theo ngày</div>
            <div class="text-muted">Tổng hợp doanh thu & hóa đơn trong ngày</div>
        </div>
        <div class="d-flex gap-2">
            <a asp-action="Month" class="btn btn-outline-primary">Tháng</a>
            <a asp-action="Year" class="btn btn-outline-primary">Năm</a>
        </div>
    </div>

    @if (ViewBag.ErrorMessage != null)
    {
        <div class="alert alert-danger">@ViewBag.ErrorMessage</div>
    }

    <div class="card mb-3">
        <div class="card-body">
            <form method="get" class="row g-2 align-items-end">
                <div class="col-md-4">
                    <label class="form-label">Chọn ngày</label>
                    <input type="date" name="date" class="form-control" value="@(Model.SelectedDate?.ToString("yyyy-MM-dd") ?? DateTime.Today.ToString("yyyy-MM-dd"))" />
                </div>
                <div class="col-md-3 d-grid">
                    <button class="btn btn-success" type="submit"><i class="bi bi-search"></i> Xem</button>
                </div>
            </form>
        </div>
    </div>

    @await Html.PartialAsync("_ReportSummaryCards", Model)

    <div class="row g-3 mt-1">
        <div class="col-lg-5">
            @await Html.PartialAsync("_ReportTopDrugs", Model)
        </div>
        <div class="col-lg-7">
            @await Html.PartialAsync("_ReportInvoices", Model)
        </div>
    </div>
</div>
